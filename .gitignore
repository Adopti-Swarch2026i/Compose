.env

# k6 raw JSON outputs (multi-MB, regenerated on every run)
tests/results/*.json

# Runtime log files generados por docker compose logs
logs_*.txt
*.log

# Resultados de tests de rendimiento (regenerados en cada ejecución)
tests/results/*.log
tests/results/*.txt
tests/results/charts/
