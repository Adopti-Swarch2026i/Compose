worker_processes auto;

events {
    worker_connections 1024;
}

stream {
    upstream pets_pool {
        least_conn;
        server pets-service-1:8443 max_fails=3 fail_timeout=10s;
        server pets-service-2:8443 max_fails=3 fail_timeout=10s;
        server pets-service-3:8443 max_fails=3 fail_timeout=10s;
    }

    server {
        listen 8443;
        proxy_pass pets_pool;
        proxy_connect_timeout 5s;
        proxy_timeout        60s;
    }
}
